{
  "name": "game-recommendation-workspace",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "install": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "setup": "npm run setup --prefix backend",
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
