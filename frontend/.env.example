# Leave empty when frontend and backend are served from the same domain.
# Example for separate API domain: VITE_API_URL="https://api.yourdomain.com/api"
VITE_API_URL=""

# Google Analytics 4 measurement ID (for example: G-XXXXXXXXXX).
VITE_GA_MEASUREMENT_ID=""
